<h1>New User Review</h1>

<%= render 'form' %>

<%= link_to 'Back', user_reviews_path %>

<%= form_for @user_review, url: {action: "create"}, html: {class: "nifty_form"} do |f| %>
    <%= f.text_field :comment %>
    <%= f.submit "Create" %>
<% end %>