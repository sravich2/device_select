<%= form_for @product, :html => { :class => "form-horizontal product" } do |f| %>

    <% if @product.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @product.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :processor, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :processor, :class => 'form-control' %>
    </div>
    <%= error_span(@product[:processor]) %>
  </div>
  <div class="control-group">
    <%= f.label :screen_size, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :screen_size, :class => 'form-control' %>
    </div>
    <%= error_span(@product[:screen_size]) %>
  </div>
  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
    <%= error_span(@product[:name]) %>
  </div>
  <div class="control-group">
    <%= f.label :memory, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :memory, :class => 'form-control' %>
    </div>
    <%= error_span(@product[:memory]) %>
  </div>
  <div class="control-group">
    <%= f.label :camera, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :camera, :class => 'form-control' %>
    </div>
    <%= error_span(@product[:camera]) %>
  </div>
  <div class="control-group">
    <%= f.label :battery, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :battery, :class => 'form-control' %>
    </div>
    <%= error_span(@product[:battery]) %>
  </div>
  <!--<div class="control-group">-->
    <!--<%# f.label :img_url, :class => 'control-label' %>-->
    <!--<div class="controls">-->
      <!--<%# f.text_field :img_url, :class => 'form-control' %>-->
    <!--</div>-->
    <!--<%# error_span(@product[:img_url]) %>-->
  <!--</div>-->


  <%= f.submit nil, :class => 'btn btn-primary new-button' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            products_path, :class => 'btn btn-default new-button' %>

<% end %>
