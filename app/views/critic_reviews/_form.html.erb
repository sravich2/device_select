<%= form_for(@critic_review) do |f| %>
  <% if @critic_review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@critic_review.errors.count, "error") %> prohibited this critic_review from being saved:</h2>

      <ul>
      <% @critic_review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
